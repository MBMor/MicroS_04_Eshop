<UserControl x:Class="Eshop.Operations.Desktop.Views.PaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Width="120"
                    Style="{StaticResource ToolbarButtonStyle}"
                    Command="{Binding LoadPaymentsCommand}"
                    Content="Refresh payments" />
            <Button Width="80"
                    Margin="8,0,0,0"
                    Style="{StaticResource ToolbarButtonStyle}"
                    Command="{Binding LoadPaymentsCancelCommand}"
                    Content="Cancel" />
            <TextBlock Margin="16,0,0,0"
                       VerticalAlignment="Center"
                       Style="{StaticResource MutedTextStyle}"
                       Text="{Binding StatusText}" />
        </StackPanel>

        <Border Grid.Row="1"
                Margin="0,12,0,12"
                Style="{StaticResource ErrorPanelBorderStyle}"
                Visibility="{Binding HasError,
                             Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Style="{StaticResource ErrorTitleTextStyle}"
                           Text="Payments request failed" />
                <TextBlock Margin="0,4,0,0"
                           Text="{Binding ErrorMessage}"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <TextBlock HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Style="{StaticResource MutedTextStyle}"
                           Text="No payments were returned."
                           Visibility="{Binding IsEmpty,
                               Converter={StaticResource BooleanToVisibilityConverter}}" />

                <DataGrid ItemsSource="{Binding Payments}"
                          SelectedItem="{Binding SelectedPayment, Mode=TwoWay}"
                          Style="{StaticResource OperationalDataGridStyle}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Status"
                                            Binding="{Binding Status}"
                                            Width="*" />
                        <DataGridTextColumn Header="Amount"
                                            Binding="{Binding Amount,
                                                      StringFormat={}{0:N2}}"
                                            Width="*" />
                        <DataGridTextColumn Header="Currency"
                                            Binding="{Binding Currency}"
                                            Width="80" />
                        <DataGridTextColumn Header="Method"
                                            Binding="{Binding PaymentMethod}"
                                            Width="*" />
                        <DataGridTextColumn Header="Order ID"
                                            Binding="{Binding OrderId}"
                                            Width="2*" />
                        <DataGridTextColumn Header="Created"
                                            Binding="{Binding CreatedAtUtc,
                                                      StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}"
                                            Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <GridSplitter Grid.Column="1"
                          Width="4"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Stretch" />

            <Border Grid.Column="2"
                    Style="{StaticResource PanelBorderStyle}">
                <Grid>
                    <TextBlock HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Text="Select a payment to view details.">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock"
                                   BasedOn="{StaticResource MutedTextStyle}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedPayment}"
                                                 Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <ContentControl Content="{Binding SelectedPayment}">
                        <ContentControl.ContentTemplate>
                            <DataTemplate>
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <StackPanel>
                                        <TextBlock Style="{StaticResource PageTitleTextStyle}"
                                                   Text="Payment details" />
                                        <TextBlock Margin="0,16,0,0"
                                                   Style="{StaticResource FieldLabelTextStyle}"
                                                   Text="Payment ID" />
                                        <TextBlock Text="{Binding Id}" TextWrapping="Wrap" />
                                        <TextBlock Margin="0,12,0,0"
                                                   Style="{StaticResource FieldLabelTextStyle}"
                                                   Text="Order ID" />
                                        <TextBlock Text="{Binding OrderId}" TextWrapping="Wrap" />
                                        <TextBlock Margin="0,12,0,0"
                                                   Style="{StaticResource FieldLabelTextStyle}"
                                                   Text="Customer ID" />
                                        <TextBlock Text="{Binding CustomerId}" TextWrapping="Wrap" />
                                        <TextBlock Margin="0,12,0,0"
                                                   Style="{StaticResource FieldLabelTextStyle}"
                                                   Text="Status" />
                                        <TextBlock Text="{Binding Status}" />
                                        <TextBlock Margin="0,12,0,0"
                                                   Style="{StaticResource FieldLabelTextStyle}"
                                                   Text="Amount" />
                                        <TextBlock>
                                            <Run Text="{Binding Amount, StringFormat={}{0:N2}}" />
                                            <Run Text=" " />
                                            <Run Text="{Binding Currency}" />
                                        </TextBlock>
                                        <TextBlock Margin="0,12,0,0"
                                                   Style="{StaticResource FieldLabelTextStyle}"
                                                   Text="Payment method" />
                                        <TextBlock Text="{Binding PaymentMethod}" />
                                        <TextBlock Margin="0,12,0,0"
                                                   Style="{StaticResource FieldLabelTextStyle}"
                                                   Text="Failure reason" />
                                        <TextBlock Text="{Binding FailureReason, TargetNullValue=—}"
                                                   TextWrapping="Wrap" />
                                        <TextBlock Margin="0,12,0,0"
                                                   Style="{StaticResource FieldLabelTextStyle}"
                                                   Text="Created UTC" />
                                        <TextBlock Text="{Binding CreatedAtUtc}" />
                                        <TextBlock Margin="0,12,0,0"
                                                   Style="{StaticResource FieldLabelTextStyle}"
                                                   Text="Processed UTC" />
                                        <TextBlock Text="{Binding ProcessedAtUtc, TargetNullValue=—}" />
                                    </StackPanel>
                                </ScrollViewer>
                            </DataTemplate>
                        </ContentControl.ContentTemplate>
                    </ContentControl>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
