# Empty value means same-origin API calls.
# In Vite development mode, /api is proxied to VITE_DEV_PROXY_TARGET.
VITE_API_BASE_URL=

# Used only by Vite development server proxy.
VITE_DEV_PROXY_TARGET=http://localhost:5080
